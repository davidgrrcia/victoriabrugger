/** @type {import('prettier').Config} */
const config = {
  semi: true,
  trailingComma: "es5",
  singleQuote: false,
  tabWidth: 2,
  useTabs: false,
  printWidth: 80,
  plugins: [],
};

export default config;
